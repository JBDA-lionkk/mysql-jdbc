<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yyww.mysql.jdbc.dao.MainOrderDao">

    <resultMap type="com.yyww.mysql.jdbc.entity.MainOrderEntity" id="MainOrderMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="orderId" column="order_id" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="price" column="price" jdbcType="VARCHAR"/>
        <result property="deleted" column="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MainOrderMap">
        select id,
               order_id,
               create_time,
               update_time,
               price,
               deleted
        from demo.main_order
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MainOrderMap">
        select id,
               order_id,
               create_time,
               update_time,
               price,
               deleted
        from demo.main_order limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MainOrderMap">
        select
        id, order_id, create_time, update_time, price, deleted
        from demo.main_order
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="orderId != null and orderId != ''">
                and order_id = #{orderId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="deleted != null">
                and deleted = #{deleted}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into demo.main_order(order_id, create_time, update_time, price, deleted)
        values (#{orderId}, #{createTime}, #{updateTime}, #{price}, #{deleted})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into demo.main_order(order_id, create_time, update_time, price, deleted)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.orderId}, #{entity.createTime}, #{entity.updateTime}, #{entity.price}, #{entity.deleted})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into demo.main_order(order_id, create_time, update_time, price, deleted)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.orderId}, #{entity.createTime}, #{entity.updateTime}, #{entity.price}, #{entity.deleted})
        </foreach>
        on duplicate key update
        order_id = values(order_id) , create_time = values(create_time) , update_time = values(update_time) , price =
        values(price) , deleted = values(deleted)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update demo.main_order
        <set>
            <if test="orderId != null and orderId != ''">
                order_id = #{orderId},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="deleted != null">
                deleted = #{deleted},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from demo.main_order
        where id = #{id}
    </delete>

</mapper>

